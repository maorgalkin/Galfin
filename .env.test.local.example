# Integration Test Environment Variables
# ========================================
#
# Copy this file to .env.test.local and fill in your test Supabase credentials.
# The .env.test.local file is gitignored and should never be committed.
#
# RECOMMENDED: Use a separate Supabase project for testing to avoid
# polluting your production database.

# Test Supabase Project URL
# Find this in: Supabase Dashboard > Settings > API > Project URL
VITE_SUPABASE_TEST_URL=https://your-test-project.supabase.co

# Test Supabase Anonymous Key
# Find this in: Supabase Dashboard > Settings > API > Project API keys > anon/public
VITE_SUPABASE_TEST_ANON_KEY=your-anon-key-here

# Test Supabase Service Role Key (REQUIRED for full test functionality)
# Find this in: Supabase Dashboard > Settings > API > Project API keys > service_role
# 
# ⚠️ WARNING: This key bypasses RLS! Never commit it!
#
# Used for:
# - Creating test users without email verification
# - Cleaning up test data (bypasses RLS)
# - Cross-user data verification
VITE_SUPABASE_TEST_SERVICE_ROLE_KEY=your-service-role-key-here
